function main()

    ? "Harbour =  Harb  ->  " + iif( "Harbour" =  "Harb", "sim", "não" )
    ? "Harbour == Harb  ->  " + iif( "Harbour" == "Harb", "sim", "não" )
    ? "O = verifica se a esquerda começa com a direita; == exige igualdade total (igualdade absoluta)."

return nil